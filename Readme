A plugin with the deprecated db:push and db:pull commands. Using this is unsupported and not encouraged. It will break when the taps servers are deprovsioned on 2014-02-06.

Use pg:push and pg:pull instead.
